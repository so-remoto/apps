{
  "name": "apps",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev-frontend\" \"npm run dev-backend\" \"npm run dev-package\"",
    "dev-frontend": "cd frontend && npm run dev",
    "dev-backend": "cd backend && npm run dev",
    "dev-package": "cd packages/core && npm run dev",
    "build-frontend": "cd frontend && npm run build",
    "build-backend": "cd backend && npm run build",
    "build-core": "cd packages/core && npm build",
    "build": "concurrently \"npm run build-frontend\" \"npm run build-backend\" \"npm run build-core\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "uuid": "^11.0.3"
  }
}
